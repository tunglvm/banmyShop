//Sử dụng react router dom 
//Giúp tạo website nhiều trang (multi-page logic) nhưng vẫn chạy như Single Page Application (SPA)

export const ROUTER = { //quản lý tất cả đường dẫn (route) ở 1 nơi duy nhất
    USER: {
        HOME: "", //index route (route mặc định)
        PROFILE: "profile", //eg: 
    }
}